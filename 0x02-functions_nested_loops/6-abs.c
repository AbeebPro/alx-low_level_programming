#include "main.h"
/**
 * _abs - prints tha absolute value of an integer
 * @r: a formal paramter of type int
 *
 * Return: Returns absolute value
 */
int _abs(int r)
{
	if (r >= 0)
		return (r);
	else
		return (r*-1);
}
