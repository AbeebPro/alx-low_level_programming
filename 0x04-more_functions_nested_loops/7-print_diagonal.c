#include "main.h"
/**
 * print_diagonal - prints diagonal
 * @n: parameter of int type
 *
 * Return: Nothing
 */
void print_diagonal(int n)
{
	int i, j, k;

	for (i = 0; i < n; i++)
	{
		if (n > 0)
		{
			for (
		}
	}
}
