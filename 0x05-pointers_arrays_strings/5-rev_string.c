#include "main.h"
/**
 * rev_string - reveese a string
 * @s: A pointer to char
 *
 * Return: Nothing
 */
void rev_string(char *s)
{
	int i = 0, len = 0;

	while (s[i] != '\0')
	{
		len++;
		i++;
	}

	for (i = (len - i); i >= 0; i--)
	{	
		
	}
}
