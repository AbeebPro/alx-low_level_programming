#include <stdio.h>
/**
 * main - function name
 * @argc: number of entry
 * @argv: string array of entry
 * Return: 0
 */
int main(int argc, char *argv[])
{
	argc = 0;
	printf("%s\n", argv[argc]);
	return (0);
}
